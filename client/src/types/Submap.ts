
export class Submap{
    id: number;
    path: string;
    width: number;
    height: number;

    constructor(id: number, path: string, width: number, height: number){
        this.id = id;
        this.path = path;
        this.width = width;
        this.height = height;
    }
}