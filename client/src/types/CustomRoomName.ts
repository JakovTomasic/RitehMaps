
export class CustomRoomName {
    roomId: number;
    customName: string;

    constructor(roomId: number, customName: string) {
        this.roomId = roomId;
        this.customName = customName;
    }
}
