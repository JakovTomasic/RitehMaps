
export class Professor {
    id: number;
    name: string;
    roomId: number;

    constructor(id: number, name: string, roomId: number) {
        this.id = id;
        this.name = name;
        this.roomId = roomId;
    }
}
