## Installation

```bash
$ yarn install
```

## Running the app

```bash
# development/watch mode
$ yarn run dev
```

## Tests

Jest (via ts-jest), test files in `tests/`:

```bash
yarn run test              # all tests
yarn run test -- --watch   # watch mode
yarn run test -- tests/findPathWithDijkstra.test.ts   # a single file
```
